# Тема 5. Базовые коллекции: словари, кортежи
Отчет по Теме #6 выполнила:
- Турылева Диана Игоревна
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |

## Лабораторная работа №1
### В школе, где вы учились, узнали, что вы крутой программист и попросили написать программу для учителей, которая будет при вводе кабинета писать для него ключ доступа и статус, занят кабинет или нет. При написании программы необходимо использовать словарь (dict), который на вход получает номер кабинета, а выводит необходимую информацию. Если кабинета, который вы ввели нет в словаре, то в консоль в виде значения ключа нужно вывести “None” и виде статуса вывести “False”.
```python
request = int(input('Введите номер кабинета: '))

dictionary = {
    101: {'key':1234, 'access':True},
    102: {'key':1337, 'access':True},
    103: {'key':8943, 'access':True},
    104: {'key':5555, 'access':False},
    None: {'key':None, 'access':False},
}

response = dictionary.get(request)
if not response:
    response = dictionary[None]
    key = response.get('key')
    access = response.get('access')
print(key, access)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_6/pic/lab_1.png)

## Выводы

Эта программа проверяет наличие кабинета в словаре и выводит ключ и статус доступа, либо значения по умолчанию при отсутствии кабинета:

- request = int(input('Введите номер кабинета: ')) — считывает номер кабинета, введённый пользователем, и преобразует ввод в целое число.
- dictionary = {...} — создаёт словарь с кабинетами, где каждому кабинету соответствует словарь с ключом и флагом доступа. Ключ None — запасной вариант для неизвестных кабинетов.
- response = dictionary.get(request) — пытается получить значения по введённому ключу (кабинету). Если ключ не найден, вернёт None.
- if not response: — проверяет, если ответ отсутствует (кабинет не найден),
- response = dictionary[None] — устанавливает response равным значению для неизвестного кабинета.
- key = response.get('key') — получает значение ключа из response.
- access = response.get('access') — получает значение доступа из response.
- print(key, access) — выводит ключ и статус доступа для запрошенного кабинета или для неизвестного.

## Лабораторная работа №2
### Алексей решил создать самый большой словарь в мире. Для этого он придумал функцию dict_maker (**kwargs), которая принимает неограниченное количество параметров «ключ: значение» и обновляет созданный им словарь my_dict, состоящий всего из одного элемента «first» со значением «so easy». Помогите Алексею создать данную функцию.

```python
from pprint import pprint

my_dict = {'first': 'so easy'}

def dict_maker(**kwargs):
    my_dict.update(**kwargs)

dict_maker(a1=1,a2=20, a3=54, a4=13)
dict_maker(name = 'Михаил', age = 31, weight = 70,eyes_color = 'blue')
pprint(my_dict)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_6/pic/lab_2.png)

## Выводы

 Функция обновляет глобальный словарь новыми парами ключ-значение, добавляя переданные аргументы:

- from pprint import pprint — импортирует функцию pprint для удобного форматированного вывода словарей.
- my_dict = {'first': 'so easy'} — создаёт словарь с одним элементом.
- def dict_maker(**kwargs): — объявляет функцию, принимающую произвольное число именованных аргументов.
- my_dict.update(**kwargs) — обновляет глобальный словарь my_dict переданными аргументами, добавляя или изменяя пары ключ-значение.
- dict_maker(a1=1,a2=20, a3=54, a4=13) — вызывает функцию, добавляя четыре новых элемента в my_dict.
- dict_maker(name = 'Михаил', age = 31, weight = 70,eyes_color = 'blue') — снова обновляет my_dict с новыми элементами.
- pprint(my_dict) — выводит итоговый словарь my_dict в удобочитаемом формате.

## Лабораторная работа №3
### Для решения некоторых задач бывает необходимо разложить строку на отдельные символы. Мы знаем что это можно сделать при помощи split(), у которого более гибкая настройка для разделения для этого, но если нам нужно посимвольно разделить строку без всяких условий, то для этого мы можем использовать кортежи (tuple). Для этого напишем любую строку, которую будем делить и “обвернем” ее в tuple и дальше мы можем как нам угодно с ней работать, например, сделать ее списком (тогда получится полный аналог split()) или же работать с ним дальше, как с кортежем.
```python
input_string = 'HelloWorld!'
result = tuple(input_string)
print(result)
print(list(result))
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_6/pic/lab_3.png)

## Выводы

Эта программа преобразует строку в кортеж символов и выводит его, а также список из тех же символов:

- input_string = 'HelloWorld!' — создаёт строку с текстом 'HelloWorld!'.
- result = tuple(input_string) — преобразует строку в кортеж, где каждый символ — отдельный элемент.
- print(result) — выводит кортеж символов ('H', 'e', 'l', 'l', 'o', 'W', 'o', 'r', 'l', 'd', '!').
- print(list(result)) — выводит список из тех же символов, преобразованный из кортежа.

## Лабораторная работа №4
### Вовочка решил написать крутую функцию, которая будет писать имя, возраст и место работы, но при этом на вход этой функции будет поступать кортеж. Помогите Вовочке написать эту программу.

```python
def personal_info(name, age, company = 'unnamed'):
    print(f"Имя: {name} Возраст: {age} Компания: {company}")

tom = ("Григорий", 22)
personal_info(*tom)

bob = ("Георгий",41,"Yandex")
personal_info(*bob)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_6/pic/lab_4.png)

## Выводы

Функция выводит персональную информацию с обязательными и необязательным параметрами, используя распаковку кортежа:

- def personal_info(name, age, company='unnamed'): — объявляет функцию с обязательными аргументами name и age и необязательным company со значением по умолчанию 'unnamed'.
- print(f"Имя: {name} Возраст: {age} Компания: {company}") — выводит отформатированную строку с переданными параметрами.
- tom = ("Григорий", 22) — создаёт кортеж с именем и возрастом.
- personal_info(*tom) — распаковывает кортеж tom в позиционные аргументы функции. Так как company не передан, используется значение по умолчанию.
- bob = ("Георгий", 41, "Yandex") — создаёт кортеж с тремя элементами: имя, возраст, компания.
- personal_info(*bob) — распаковывает кортеж bob и передаёт все три аргумента функции, выводя полную информацию.

## Лабораторная работа №5
### Для сопровождения первых лиц государства X нужен кортеж, но никто не может определиться с порядком машин, поэтому вам нужно написать функцию, которая будет сортировать кортеж, состоящий из целых чисел по возрастанию, и возвращает его. Если хотя бы один элемент не является целым числом, то функция возвращает исходный кортеж.
```python
def tuple_sort(tpl):
    for elm in tpl:
        if not isinstance(elm, int):
            return tpl 
    return tuple(sorted(tpl))

if __name__ == '__main__':
    print(tuple_sort((5,5,3,1,9)))
print(tuple_sort((5,5,2.1,'1',9)))
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_6/pic/lab_5.png)

## Выводы

Функция сортирует кортеж только если все его элементы целые числа, иначе возвращает исходный кортеж без изменений:

- def tuple_sort(tpl): — объявляет функцию tuple_sort, принимающую кортеж tpl.
- for elm in tpl: — перебирает все элементы в кортеже.
- if not isinstance(elm, int): — проверяет, если элемент не целое число,
- return tpl — возвращает исходный кортеж без изменений.
- return tuple(sorted(tpl)) — если все элементы — целые числа, возвращает новый кортеж с отсортированными элементами.
- if __name__ == '__main__': — проверяет, что скрипт запущен напрямую, а не импортирован.
- print(tuple_sort((5,5,3,1,9))) — вызывает функцию с кортежем из целых чисел и выводит отсортированный кортеж (1,3,5,5,9).
- print(tuple_sort((5,5,2.1,'1',9))) — вызывается с кортежем, где есть нецелые числа (float и строка), функция возвращает исходный кортеж без изменений.


## Самостоятельная работа №1
### При создании сайта у вас возникла потребность обрабатывать данные пользователя в странной форме, а потом переводить их в нужные вам форматы. Вы хотите принимать от пользователя последовательность чисел, разделенных пробелом, а после переформатировать эти данные в список и кортеж. Реализуйте вашу задумку. Для получения начальных данных используйте input(). Результатом программы будет выведенный список и кортеж из начальных данных.

```python
data = input("Введите числа, разделённые пробелом: ")
list_numbers = [int(x) for x in data.split()]
tuple_numbers = tuple(list_numbers)

print("Список:", list_numbers)
print("Кортеж:", tuple_numbers)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_6/pic/sam_1.png)

## Выводы

Эта программа считывает числа из строки, преобразует их в список и кортеж, и выводит оба варианта:

- data = input("Введите числа, разделённые пробелом: ") — запрашивает у пользователя строку чисел, разделённых пробелами.
- list_numbers = [int(x) for x in data.split()] — разбивает строку на элементы, преобразует каждый в целое число и создаёт список.
- tuple_numbers = tuple(list_numbers) — преобразует список целых чисел в кортеж.
- print("Список:", list_numbers) — выводит полученный список чисел.
- print("Кортеж:", tuple_numbers) — выводит полученный кортеж чисел.

## Самостоятельная работа №2
### Николай знает, что кортежи являются неизменяемыми, но он очень упрямый и всегда хочет доказать, что он прав. Студент решил создать функцию, которая будет удалять первое появление определенного элемента из кортежа по значению и возвращать кортеж без него. Попробуйте повторить шедевр не признающего авторитеты начинающего программиста. Но учтите, что Николай не всегда уверен в наличии элемента в кортеже (в этом случае кортеж вернется функцией в исходном виде).
```python
def remove(t, el):
    if el not in t:
        return t
    index = t.index(el)
    return t[:index] + t[index+1:]

print(remove((1, 2, 3), 1))
print(remove((1, 2, 3, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2), 3))
print(remove((2, 4, 6, 6, 4, 2), 9))
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_6/pic/sam_2.png)

## Выводы

Эта функция удаляет первое вхождение заданного элемента из кортежа и возвращает изменённый кортеж:

- def remove(t, el): — объявляет функцию remove для удаления первого вхождения элемента el из кортежа t.
- if el not in t: — если элемент el отсутствует в кортеже,
- return t — возвращает исходный кортеж без изменений.
- index = t.index(el) — находит индекс первого вхождения элемента el.
- return t[:index] + t[index+1:] — возвращает новый кортеж, состоящий из частей до и после найденного индекса, исключая элемент el.
- print(remove((1, 2, 3), 1)) — выводит кортеж (2, 3), удалив первый 1.
- print(remove((1, 2, 3, 1, 2, 3, 4, 5, 2, 3, 4, 2, 4, 2), 3)) — выводит кортеж с удалённым первым 3.
- print(remove((2, 4, 6, 6, 4, 2), 9)) — поскольку 9 нет, возвращает исходный кортеж.
  
## Самостоятельная работа №3
### Ребята поспорили кто из них одним нажатием на numpad наберет больше повторяющихся цифр, но не понимают, как узнать победителя. Вам им нужно в этом помочь. Дана строка в виде случайной последовательности чисел от 0 до 9 (длина строки минимум 15 символов). Требуется создать словарь, который в качестве ключей будет принимать данные числа (т. е. ключи будут типом int), а в качестве значений – количество этих чисел в имеющейся последовательности. Для построения словаря создайте функцию, принимающую строку из цифр. Функция должна возвратить словарь из 3-х самых часто встречаемых чисел, также эти значения нужно вывести в порядке возрастания ключа.

```python
from collections import Counter

def three(s):
    counts = Counter(int(digit) for digit in s)
    most_common = counts.most_common(3)
    top = sorted([item[0] for item in most_common])
    return top

input_str = "583902748193475"
result = three(input_str)
print("Три самых часто встречающихся числа:", result)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_6/pic/sam_3.png)

## Выводы

Программа находит три самых часто встречающихся цифры в строке и выводит их в отсортированном виде:

- from collections import Counter — импортирует класс Counter для подсчёта частот элементов.
- def three(s): — объявляет функцию three, которая принимает строку s.
- counts = Counter(int(digit) for digit in s) — создаёт счётчик цифр, преобразуя каждый символ строки в число.
- most_common = counts.most_common(3) — находит три наиболее часто встречающихся числа с их частотами.
- top = sorted([item[0] for item in most_common]) — создаёт список из трёх чисел и сортирует его по возрастанию.
- return top — возвращает отсортированный список из трёх самых частых чисел.
- input_str = "583902748193475" — исходная строка с цифрами.
- result = three(input_str) — вызывает функцию для подсчёта трёх наиболее частых цифр.
- print("Три самых часто встречающихся числа:", result) — выводит результат. Например, [3,4,5].

## Самостоятельная работа №4
### Ваш хороший друг владеет офисом со входом по электронным картам, ему нужно чтобы вы написали программу, которая показывала в каком порядке сотрудники входили и выходили из офиса. Определение сотрудника происходит по id. Напишите функцию, которая на вход принимает кортеж и случайный элемент (id), его можно придумать самостоятельно. Требуется вернуть новый кортеж, начинающийся с первого появления элемента в нем и заканчивающийся вторым его появлением включительно.
### Если элемента нет вовсе – вернуть пустой кортеж.
### Если элемент встречается только один раз, то вернуть кортеж, который начинается с него и идет до конца исходного.

```python
def func(event, id):
    try:
        first_idx = event.index(id)
    except ValueError:
        return ()

    try:
        second_idx = event.index(id, first_idx + 1)
        return event[first_idx:second_idx + 1]
    except ValueError:
        return event[first_idx:]

print(func((1, 2, 3), 8))
print(func((1, 8, 3, 4, 8, 8, 9, 2), 8))
print(func((1, 2, 8, 5, 1, 2, 9), 8))
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_6/pic/sam_4.png)

## Выводы

 Эта функция возвращает срез кортежа между двумя вхождениями заданного элемента или от первого вхождения до конца, если второе отсутствует:

- def func(event, id): — объявляет функцию func, принимающую кортеж event и значение id.
- try: — начинает блок для обработки ошибок.
- first_idx = event.index(id) — пытается найти индекс первого вхождения id в кортеже event; если не найдено — выкинет ошибку ValueError.
- except ValueError: — если id нет в event,
- return () — возвращает пустой кортеж.
- try: — снова пытается выполнить следующий блок.
- second_idx = event.index(id, first_idx + 1) — пытается найти второе вхождение id, начиная поиск после первого найденного индекса.
- return event[first_idx:second_idx + 1] — если второе вхождение найдено, возвращает срез кортеже от первого до второго вхождения включительно.
- except ValueError: — если второго вхождения нет,
- return event[first_idx:] — возвращает срез от первого вхождения id до конца кортежа.
- print(func((1, 2, 3), 8)) — выводит пустой кортеж, так как 8 нет в исходном.
- print(func((1, 8, 3, 4, 8, 8, 9, 2), 8)) — выводит кортеж с элементами от первого до второго вхождения 8: (8, 3, 4, 8).
- print(func((1, 2, 8, 5, 1, 2, 9), 8)) — выводит срез от первого вхождения 8 до конца: (8, 5, 1, 2, 9).

## Самостоятельная работа №5
### Самостоятельно придумайте и решите задачу, в которой будут обязательно использоваться кортеж или список. Проведите минимум три теста для проверки работоспособности вашей задачи.
```python
def uniqel(lst):
    elements = set(lst)
    return len(elements), list(elements)

test1 = [1, 2, 2, 3, 4, 4, 5]
test2 = [10, 10, 10, 20, 30]
test3 = [7, 8, 9, 7, 8]

print(uniqel(test1))
print(uniqel(test2))
print(uniqel(test2))
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_6/pic/sam_5.png)

## Выводы

Функция определяет количество уникальных элементов списка и возвращает их количество и список уникальных значений:

- def uniqel(lst): — объявляет функцию uniqel, принимающую список lst.
- elements = set(lst) — создаёт множество из элементов списка, убирая дубликаты.
- return len(elements), list(elements) — возвращает кортеж с количеством уникальных элементов и списком этих уникальных элементов (порядок не гарантирован).
- test1 = [1, 2, 2, 3, 4, 4, 5] — создаёт тестовый список с дубликатами.
- test2 = [10, 10, 10, 20, 30] — другой тестовый список с повторяющимися элементами.
- test3 = [7, 8, 9, 7, 8] — третий тестовый список с дубликатами.
- print(uniqel(test1)) — выводит количество уникальных элементов и список уникальных, например (5, [1, 2, 3, 4, 5]).
- print(uniqel(test2)) — выводит (3, [10, 20, 30]).
- print(uniqel(test2)) — повторный вызов с test2, результат тот же, (3, [10, 20, 30]).


## Общие выводы по теме

Изучив словари и кортежи в Python, я поняла, что словари — это структуры данных, использующие пары "ключ-значение" для быстрого доступа к информации, что удобно для хранения связанных данных. Кортежи — это неизменяемые последовательности, предназначенные для хранения фиксированного набора элементов, что делает их подходящими для константных данных. Я научилась создавать словари и кортежи, обращаться к их элементам, а также использовать методы для работы с данными в словарях. Поняла важность использования кортежей как ключей в словарях из-за их неизменяемости. Эти знания помогут мне писать более эффективный код и лучше понимать работу с данными.
