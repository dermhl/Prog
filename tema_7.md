# Тема 7. Работа с файлами(ввод и вывод)
Отчет по Теме #7 выполнила:
- Турылева Диана Игоревна
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |  |
| Задание 7 | + |  |
| Задание 8 | + |  |
| Задание 9 | + |  |
| Задание 10 | + |  |

## Лабораторная работа №1
### Составьте текстовый файл и положите его в одну директорию с программой на Python. Текстовый файл должен состоять минимум из двух строк.

### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/lab_1.png)

## Выводы

Научилась добавлять заполненный текстовый файл в директорию с программой Python для дальнейшей работы с ним.

## Лабораторная работа №2
### Напишите программу, которая выведет только первую строку из вашего файла, при этом используйте конструкцию open()/close(). 
```python
f = open('input.txt', 'r')
print(f.readline())
f.close()
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/lab_2.png)

## Выводы

Эта программа открывает файл и выводит первую строку:

- f = open('input.txt', 'r') — открывает файл "input.txt" в режиме чтения.
- print(f.readline()) — читает и выводит первую строку из файла (до символа перевода строки).
- f.close() — закрывает файл, освобождая ресурсы.

## Лабораторная работа №3
### Напишите программу, которая выведет все строки из вашего файла в массиве, при этом используйте конструкцию open()/close().
```python
f = open('input.txt', 'r')
print(f.readlines())
f.close()
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/lab_3.png)

## Выводы

Эта программа читает весь файл и выводит список всех строк:

- f = open('input.txt', 'r') — открывает файл для чтения.
- print(f.readlines()) — читает все строки файла и возвращает список строк, выводит этот список.
- f.close() — закрывает файл.

## Лабораторная работа №4
### Напишите программу, которая выведет все строки из вашего файла в массиве, при этом используйте конструкцию with open().
```python
with open('input.txt') as f:
    print(f.readlines())
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/lab_4.png)

## Выводы

Эта программа с помощью контекстного менеджера выводит список всех строк файла:

- with open('input.txt') as f — открывает файл "input.txt" для чтения и гарантирует его закрытие после блока.
- print(f.readlines()) — выводит список всех строк файла.

## Лабораторная работа №5
### Напишите программу, которая выведет каждую строку из вашего файла отдельно, при этом используйте конструкцию with open().
```python
with open('input.txt') as f:
    for line in f:
        print(line)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/lab_5.png)

## Выводы

 Эта программа построчно выводит содержимое файла с помощью цикла:

- with open('input.txt') as f — открывает файл на чтение с автоматическим закрытием.
- for line in f — перебирает файл построчно.
- print(line) — выводит каждую строку по отдельности, включая символы переноса строки.
  
## Лабораторная работа №6
### Напишите программу, которая будет добавлять новую строку в ваш файл, а потом выведет полученный файл в консоль. Вывод можно осуществлять любым способом. Обязательно проверьте сам файл, чтобы изменения в нем тоже отображались.
```python
with open('input.txt', 'a+') as f:
    f.write('\nIm additional line')

with open('input.txt', 'r') as f:
    result = f.readlines()
    print(result)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/lab_6.png)

## Выводы

Эта программа добавляет строку в конец файла, затем читает и выводит весь файл:

- with open('input.txt', 'a+') as f — открывает файл для чтения и дозаписи, курсор стоит в конце файла.
- f.write('\nIm additional line') — добавляет в файл новую строку с текстом.
- with open('input.txt', 'r') as f — повторно открывает файл в режиме чтения.
- result = f.readlines() — считывает все строки файла в список.
- print(result) — выводит список строк, включая добавленную.

## Лабораторная работа №7
### Напишите программу, которая перепишет всю информацию, которая была у вас в файле до этого, например, напишет любые данные из произвольно вами составленного списка. Также не забудьте проверить, что измененная вами информация сохранилась в файле.
```python
lines = ['one','two','three']
with open('input.txt', 'w') as f:
    for line in lines:
        f.write('\nCycle run'+ line)
    print('Done!')
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/lab_7.png)

## Выводы

Эта программа перезаписывает файл, записывая в него несколько строк с префиксом, и выводит сообщение о завершении:

- lines = ['one','two','three'] — создаёт список строк.
- with open('input.txt', 'w') as f — открывает файл для записи, очищая его содержимое.
- for line in lines: — перебирает элементы списка.
- f.write('\nCycle run' + line) — записывает в файл строки, каждая начинается с переноса строки и текста "Cycle run", затем добавляется элемент списка.
- print('Done!') — выводит сообщение о завершении записи.

## Лабораторная работа №8
### Выберите любую папку на своем компьютере, имеющую вложенные директории. Выведите на печать в терминал ее содержимое, как и всех подкаталогов при помощи функции print_docs(directory).
```python
import os

def print_docs(directory):
    all_files = os.walk(directory)
    for catalog in all_files:
        print(f'Папка {catalog[0]} содержит:')
        print(f'Директории: {", ".join(catalog[1])}')
        print(f'Файлы: {", ".join(catalog[2])}')
        print('-' * 40)

print_docs(r' C:\Users\1\PycharmProjects\PythonProject\tema_6')
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/lab_8.png)

## Выводы

Эта программа рекурсивно выводит содержимое всех папок и файлов в указанной директории:

- import os — импортирует модуль os для работы с файловой системой.
- def print_docs(directory): — объявляет функцию, которая принимает путь к директории.
- all_files = os.walk(directory) — создаёт генератор для обхода всех папок и файлов внутри заданной директории.
- for catalog in all_files: — итерируется по каждой папке, возвращая кортеж (путь, список папок, список файлов).
- print(f'Папка {catalog[0]} содержит:') — выводит название текущей папки.
- print(f'Директории: {", ".join(catalog[1])}') — выводит имена вложенных папок через запятую.
- print(f'Файлы: {", ".join(catalog[2])}') — выводит имена файлов в текущей папке через запятую.
- print('-' * 40) — выводит разделитель из 40 дефисов для читаемости.
- print_docs(r' C:\Users\1\PycharmProjects\PythonProject\tema_6') — вызывает функцию для указанной директории и выводит содержимое.

## Лабораторная работа №9
###Требуется реализовать функцию, которая выводит слово, имеющее максимальную длину (или список слов, если таковых несколько). Проверьте работоспособность программы на своем наборе данных.

```python
def longest_word(file):
    with open(file, encoding= 'utf-8') as f:
        word = f.read().split()
        max_length = len(max(word, key=len))
        for w in word:
            if len(w)==max_length:
                sought_word = w
        if len(sought_word)==1:
            return sought_word[0]
        return sought_word
print(longest_word('input.txt'))
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/lab_9.png)

## Выводы
Эта программа находит и возвращает самое длинное слово из текстового файла:

- def longest_word(file): — объявляет функцию, которая принимает имя файла.
- with open(file, encoding= 'utf-8') as f: — открывает файл с кодировкой UTF-8.
- word = f.read().split() — читает весь текст и разбивает его на список слов.
- max_length = len(max(word, key=len)) — находит длину самого длинного слова.
- for w in word: — перебирает каждое слово.
- if len(w)==max_length: — ищет слово с максимальной длиной.
- sought_word = w — сохраняет последнее найденное слово с максимальной длиной (если несколько — последнее).
- if len(sought_word)==1: — проверяет, если слово длиной 1 символ, возвращает его первый символ.
- return sought_word — возвращает найденное слово максимальной длины.
- print(longest_word('input.txt')) — выводит результат функции для файла input.txt.

## Лабораторная работа №10
### Требуется создать csv-файл «rows_300.csv» со следующими столбцами:
### № - номер по порядку (от 1 до 300);
### Секунда – текущая секунда на вашем ПК;
### Микросекунда – текущая миллисекунда на часах.
### Для наглядности на каждой итерации цикла искусственно приостанавливайте скрипт на 0,01 секунды.

```python
import csv
import datetime
import time

with open('rows_300.csv', 'w', encoding='utf-8', newline='') as f:
    writer = csv.writer(f)
    writer.writerow(['№','Секунда','Микросекунда'])
    for line in range(1,301):
        writer.writerow([line, datetime.datetime.now().second, datetime.datetime.now().microsecond])
        time.sleep(0.01)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/lab_10.png)

## Выводы

Эта программа создаёт CSV-файл с 300 строками, фиксируя номер строки и текущее время в секундах и микросекундах:

- import csv — импортирует модуль для работы с CSV-файлами.
- import datetime — импортирует модуль для работы с датой и временем.
- import time — импортирует модуль для управления временем (задержки).
- with open('rows_300.csv', 'w', encoding='utf-8', newline='') as f: — открывает CSV файл для записи.
- writer = csv.writer(f) — создаёт объект для записи в CSV.
- writer.writerow(['№','Секунда','Микросекунда']) — записывает заголовок таблицы.
- for line in range(1,301): — цикл от 1 до 300 для создания 300 строк.
- writer.writerow([line, datetime.datetime.now().second, datetime.datetime.now().microsecond]) — записывает строку с номером и текущими секундами и микросекундами.
- time.sleep(0.01) — задержка в 0.01 секунды между записями, чтобы значения изменялись.

## Самостоятельная работа №1
### Найдите в интернете любую статью (объем статьи не менее 200 слов), скопируйте ее содержимое в файл и напишите программу, которая считает количество слов в текстовом файле и определит самое часто встречающееся слово. Результатом выполнения задачи будет: скриншот файла со статьей, листинг кода, и вывод в консоль, в котором будет указана вся необходимая информация.

```python
import re
from collections import Counter

f = 'input.txt'
with open(f, 'r', encoding='utf-8') as file:
    text = file.read()

# Очистка текста от пунктуации и разбивка на слова
words = re.findall(r'\b\w+\b', text.lower())
total_words = len(words)
counter = Counter(words)
most_common_word, frequency = counter.most_common(1)[0]

print(f"Общее количество слов: {total_words}")
print(f"Самое часто встречающееся слово: '{most_common_word}' (встречается {frequency} раз)")
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/sam_1.png)

## Выводы

Эта программа подсчитывает общее количество слов в файле и находит самое часто встречающееся слово:

- import re — импортирует модуль для работы с регулярными выражениями.
- from collections import Counter — импортирует класс для подсчёта количества элементов.
- f = 'input.txt' — задаёт имя файла для чтения.
- with open(f, 'r', encoding='utf-8') as file: — открывает файл для чтения с кодировкой UTF-8.
- text = file.read() — читает весь текст из файла.
- words = re.findall(r'\b\w+\b', text.lower()) — находит все слова, приводя к нижнему регистру (удаляет знаки пунктуации).
- total_words = len(words) — считает общее количество слов.
- counter = Counter(words) — создаёт словарь с подсчётом частоты каждого слова.
- most_common_word, frequency = counter.most_common(1)[0] — находит самое часто встречающееся слово и его количество.
- print(f"Общее количество слов: {total_words}") — выводит общее число слов.
- print(f"Самое часто встречающееся слово: '{most_common_word}' (встречается {frequency} раз)") — выводит самое частое слово и его частоту.

## Самостоятельная работа №2
### У вас появилась потребность в ведении книги расходов, посмотрев все существующие варианты вы пришли к выводу что вас ничего не устраивает и нужно все делать самому. Напишите программу для учета расходов. Программа должна позволять вводить информацию о расходах, сохранять ее в файл и выводить существующие данные в консоль. Ввод информации происходит через консоль. Результатом выполнения задачи будет: скриншот файла с учетом расходов, листинг кода, и вывод в консоль, с демонстрацией работоспособности программы.

```python
import os

f = 'expen.txt'
def load_expenses():
    """Загрузка списка расходов из файла"""
    expenses = []
    if os.path.exists(f):
        with open(f, 'r', encoding='utf-8') as file:
            for line in file:
                line = line.strip()
                if line:
                    expenses.append(line)
    return expenses


def save_expense(expense_str):
    """Добавление новой записи расхода в файл"""
    with open(f, 'a', encoding='utf-8') as file:
        file.write(expense_str + '\n')


def add_expense():
    """Запрос у пользователя данных для нового расхода и из созранение"""
    date = input("Введите дату (например, 2023-10-20): ")
    amount = input("Введите сумму: ")

    expense_str = f"{date}| {amount}"
    save_expense(expense_str)
    print("Расход добавлен!")


def show_expenses():
    """Вывод всех сохранённых расходов"""
    expenses = load_expenses()
    if not expenses:
        print("Нет записей расходов.")
    else:
        print("\nТекущие расходы:")
        for i, expense in enumerate(expenses, 1):
            print(f"{i}. {expense}")


def main():
    while True:
        print("\nВыберите действие:")
        print("1. Добавить расход")
        print("2. Посмотреть расходы")
        choice = input()

        if choice == '1':
            add_expense()
        elif choice == '2':
            show_expenses()



if __name__ == "__main__":
    main()
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/sam_2.png)

## Выводы

Эта программа реализует учёт расходов: позволяет добавлять новые записи с датой и суммой в файл и выводить все сохранённые расходы:

- import os — подключает модуль os для работы с файловой системой.
- f = 'expen.txt' — задаёт имя файла для хранения расходов.
- def load_expenses(): — объявляет функцию для загрузки списка расходов из файла.
- if os.path.exists(f): — проверяет, существует ли файл с расходами.
- with open(f, 'r', encoding='utf-8') as file: — открывает файл на чтение с заданной кодировкой.
- for line in file: — перебирает строки файла.
- line = line.strip() — удаляет пробельные символы по краям строки.
- expenses.append(line) — добавляет непустую строку в список расходов.
- def save_expense(expense_str): — объявляет функцию для добавления новой записи расхода в файл.
- with open(f, 'a', encoding='utf-8') as file: — открывает файл в режиме добавления текста.
- file.write(expense_str + '\n') — записывает новую строку с расходом в файл.
- def add_expense(): — объявляет функцию для взаимодействия с пользователем и сохранения нового расхода.
- expense_str = f"{date}| {amount}" — формирует строку с датой и суммой через «|».
- save_expense(expense_str) — вызывает функцию для записи расхода в файл.
- def show_expenses(): — объявляет функцию для отображения всех расходов.
- expenses = load_expenses() — загружает текущий список расходов из файла.
- if not expenses: — проверяет, есть ли записи в списке.
- print("Нет записей расходов.") — выводит сообщение, если список пуст.
- print("\nТекущие расходы:") — выводит заголовок.
- for i, expense in enumerate(expenses, 1): — перебирает все расходы с нумерацией.
- def main(): — объявляет главную функцию программы с циклом меню.
- while True: — запускает бесконечный цикл для меню.
- print("\nВыберите действие:") — выводит приглашение выбрать действие.
- choice = input() — считывает выбор пользователя.
- if choice == '1': — если выбран пункт добавления, запускает функцию добавления расхода.
- elif choice == '2': — если выбран просмотр, запускает функцию показа расходов.
- if __name__ == "__main__": — условие запуска главной функции при исполнении файла напрямую.

  
## Самостоятельная работа №3
### Имеется файл input.txt с текстом на латинице. Напишите программу, которая выводит следующую статистику по тексту: количество букв латинского алфавита; число слов; число строк.
### 	Текст в файле: 
### Beautiful is better than ugly. 
### Explicit is better than implicit. 
### Simple is better than complex.
### Complex is better than complicated.
### 	Ожидаемый результат: 
### Input file contains:
### 108 letters
### 20 words
### 4 lines
```python
import re
with open('input.txt', 'r', encoding='utf-8') as f:
    lines = f.readlines()
num_lines = len(lines)
text = ''.join(lines)
words = re.findall(r'\b\w+\b', text)
num_words = len(words)


letters = re.findall(r'[A-Za-z]', text)
num_letters = len(letters)
print(f"Input file contains:\n{num_letters} letters\n{num_words} words\n{num_lines} lines")
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/sam_3.png)

## Выводы

Эта программа анализирует текстовый файл, подсчитывая и выводя количество букв, слов и строк в нём:

- import re — импортирует модуль для работы с регулярными выражениями.
- with open('input.txt', 'r', encoding='utf-8') as f: — открывает файл на чтение с кодировкой utf-8.
- lines = f.readlines() — считывает все строки файла в список.
- num_lines = len(lines) — считает количество строк в файле.
- text = ''.join(lines) — объединяет все строки в один текст.
- words = re.findall(r'\b\w+\b', text) — находит все слова в тексте с помощью регулярного выражения.
- num_words = len(words) — подсчитывает количество слов.
- letters = re.findall(r'[A-Za-z]', text) — находит все латинские буквы в тексте.
- num_letters = len(letters) — подсчитывает количество букв.
- print(f"Input file contains:\n{num_letters} letters\n{num_words} words\n{num_lines} lines") — выводит количество букв, слов и строк в файле.

## Самостоятельная работа №4
### Напишите программу, которая получает на вход предложение, выводит его в терминал, заменяя все запрещенные слова звездочками * (количество звездочек равно количеству букв в слове). Запрещенные слова, разделенные символом пробела, хранятся в текстовом файле input.txt. Все слова в этом файле записаны в нижнем регистре. Программа должна заменить запрещенные слова, где бы они ни встречались, даже в середине другого слова. Замена производится независимо от регистра: если файл input.txt содержит запрещенное слово exam, то слова exam, Exam, ExaM, EXAM и exAm должны быть заменены на ****.
### 	Запрещенные слова:
### hello email python the exam wor is
### 	Предложение для проверки:
### Hello, world! Python IS the programming language of thE future. My EMAIL is....
### PYTHON is awesome!!!!
### 	Ожидаемый результат:
### *****, ***ld! ****** ** *** programming language of *** future. My ***** **....
### ****** ** awesome!!!!

```python
import re

file = 'input.txt'
sentence = "Hello, world! Python IS the programming language of thE future. My EMAIL is....\nPYTHON is awesome!!!!"
with open(file, 'r', encoding='utf-8') as f:
    bad_words = f.read().split()
bad_words = [word.lower() for word in bad_words]

pattern = re.compile('|'.join([re.escape(word) for word in bad_words]), re.IGNORECASE)
def replace_bad_words(text):
    def replacer(match):
        word = match.group()
        if word.lower() in bad_words:
            return '*' * len(word)
        return word
    return pattern.sub(replacer, text)

result = replace_bad_words(sentence)
print(result)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/sam_4.png)

## Выводы

Эта программа заменяет в заданном тексте все плохие слова из файла на звёздочки, не учитывая регистр:

- import re — импортирует модуль для работы с регулярными выражениями.
- file = 'input.txt' — задаёт имя файла с плохими словами.
- sentence = "Hello, world!..." — задаёт исходную строку для обработки.
- with open(file, 'r', encoding='utf-8') as f: — открывает файл с плохими словами для чтения.
- bad_words = f.read().split() — считывает и разбивает содержимое файла на список плохих слов.
- bad_words = [word.lower() for word in bad_words] — приводит все плохие слова к нижнему регистру для безрегистрового сравнения.
- pattern = re.compile('|'.join([re.escape(word) for word in bad_words]), re.IGNORECASE) — создаёт регулярное выражение для поиска любых плохих слов, игнорируя регистр.
- def replace_bad_words(text): — объявляет функцию для замены плохих слов на звёздочки в тексте.
- def replacer(match): — вложенная функция, вызываемая при каждом совпадении; проверяет слово и заменяет его звёздочками той же длины, если оно плохое.
- return pattern.sub(replacer, text) — заменяет все плохие слова в тексте, вызывая replacer.
- result = replace_bad_words(sentence) — применяет функцию замены к исходному предложению.
- print(result) — выводит результат с заменёнными плохими словами.

## Самостоятельная работа №5
### Самостоятельно придумайте и решите задачу, которая будет взаимодействовать с текстовым файлом.
```python
import re
with open('input.txt', 'r', encoding='utf-8') as f:
    content = f.read()
words = re.findall(r'\b\w+\b', content)
print(f"В файле содержится {len(words)} слов.")
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_7/pic/sam_5.png)

## Выводы

Эта программа подсчитывает и выводит количество слов, содержащихся в текстовом файле:

- import re — импортирует модуль для работы с регулярными выражениями.
- with open('input.txt', 'r', encoding='utf-8') as f: — открывает файл для чтения содержимого.
- content = f.read() — считывает весь текст из файла в строку content.
- words = re.findall(r'\b\w+\b', content) — находит все слова в тексте (последовательности буквенно-цифровых символов).
- print(f"В файле содержится {len(words)} слов.") — выводит количество найденных слов в файле.


## Общие выводы по теме

Изучив тему, я освоила сохранение данных вне программы для их сохранности и доступности. Научилась открывать файлы в разных режимах (чтение, запись, добавление) и правильно закрывать их или использовать контекстные менеджеры (with). Также я узнала, как читать данные из файлов целиком, построчно или посимвольно и записывать информацию. Эти знания важны для сохранения результатов, обработки внешних данных и ведения журналов. Умение работать с файлами расширяет возможности автоматизации и решения практических задач, развивает навыки работы с внешними данными и повышает уверенность в использовании Python для реальных проектов.
