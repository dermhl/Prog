# Тема 7. Работа с файлами(ввод и вывод)
Отчет по Теме #7 выполнила:
- Турылева Диана Игоревна
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |  |
| Задание 7 | + |  |
| Задание 8 | + |  |
| Задание 9 | + |  |
| Задание 10 | + |  |

## Лабораторная работа №1
### Составьте текстовый файл и положите его в одну директорию с программой на Python. Текстовый файл должен состоять минимум из двух строк.

### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/lab_1.png)

## Выводы

Эта программа находит уникальные элементы первого множества, не принадлежащие ко второму:

- set_1 = {...} — создаёт множество цветов set_1.
- set_2 = {...} — создаёт множество set_2 с элементами, при этом дубли 'Red' игнорируются.
- print(set_1 - set_2) — выводит разность множеств, удаляя из set_1 все элементы, которые есть в set_2. Результат: {'White', 'Black', 'Pink'}.

## Лабораторная работа №2
### Напишите программу, которая выведет только первую строку из вашего файла, при этом используйте конструкцию open()/close(). 
```python
f = open('input.txt', 'r')
print(f.readline())
f.close()
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/lab_2.png)

## Выводы

Эта программа добавляет элемент в множество, при этом при работе с функцией frozenset() появляется ошибка так как это множество нельзя изменить после объявления:

При set():
- a = set('abcdefg') — создаёт множество уникальных символов из строки.
- print(a) — выводит множество символов, порядок произвольный.
- for i in range(1,5): a.add(i) — добавляет в множество числа 1, 2, 3, 4.
- print(a) — выводит обновлённое множество с символами и числами.

При frozenset():
- a = frozenset('abcdefg') — создаёт неизменяемое множество.
- print(a) — выводит frozenset символов.
- a.add(i) — пытается добавить элементы, но вызывает ошибку, потому что frozenset нельзя изменять.
- Конец программы не достигнется из-за ошибки AttributeError.

## Лабораторная работа №3
### Напишите программу, которая выведет все строки из вашего файла в массиве, при этом используйте конструкцию open()/close().
```python
f = open('input.txt', 'r')
print(f.readlines())
f.close()
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/lab_3.png)

## Выводы

Программа первый элемент списка на последний:

- def replace(...) — определяет функцию, которая принимает список.
- memory = input_list[0] — сохраняет первый элемент (не используется далее).
- input_list[0] = input_list[-1] — заменяет первый элемент на последний.
- return input_list — возвращает изменённый список.
- print(...) — выводит результат: [5, 2, 3, 4, 5].

## Лабораторная работа №4
### Напишите программу, которая выведет все строки из вашего файла в массиве, при этом используйте конструкцию with open().
```python
with open('input.txt') as f:
    print(f.readlines())
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/lab_4.png)

## Выводы

Программа использует "срез", для того чтобы вывести элементы с индексами от 2 до 6:

- a = [...] — создаёт список из чисел.
- print(a[2:6]) — выводит срез списка с 3-го по 6-й элемент (элементы с индексами 2, 3, 4, 5): [32, 57, 824, 13].

## Лабораторная работа №5
### Напишите программу, которая выведет каждую строку из вашего файла отдельно, при этом используйте конструкцию with open().
```python
with open('input.txt') as f:
    for line in f:
        print(line)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/lab_5.png)

## Выводы

Программа использует функцию useless(),чтобы найти частное от максимального элемента и длины массива:

- def useless(lst): — определяет функцию, которая берёт список.
- return max(lst)/len(lst) — вычисляет максимальное значение, делит на длину списка и возвращает результат.
- Три вызова функции выводят разные значения для трёх списков.
  
## Лабораторная работа №6
### Напишите программу, которая будет добавлять новую строку в ваш файл, а потом выведет полученный файл в консоль. Вывод можно осуществлять любым способом. Обязательно проверьте сам файл, чтобы изменения в нем тоже отображались.
```python
with open('input.txt', 'a+') as f:
    f.write('\nIm additional line')

with open('input.txt', 'r') as f:
    result = f.readlines()
    print(result)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/lab_6.png)

## Выводы

Эта программа разделяет объявленный список и определяет соответствие элементов и пременных:

- superheroes = [...] — создаёт список с именами.
- nikolay, vasiliy, ivan = superheroes — распаковывает список в три переменные.
- Три команды print выводят имена с соответствующими метками.

## Лабораторная работа №7
### Напишите программу, которая перепишет всю информацию, которая была у вас в файле до этого, например, напишет любые данные из произвольно вами составленного списка. Также не забудьте проверить, что измененная вами информация сохранилась в файле.
```python
lines = ['one','two','three']
with open('input.txt', 'w') as f:
    for line in lines:
        f.write('\nCycle run'+ line)
    print('Done!')
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/lab_7.png)

## Выводы

Эта программа находит минимальный элемент и удаляет его из массива:

- a = [...] — создаётся список с числами.
- a.sort() — сортирует список по возрастанию.
- print(...) — выводит отсортированный список.
- a.pop(0) — удаляет первый элемент списка (наименьший).
- print(...) — выводит список без наименьшего элемента.

## Лабораторная работа №8
### Выберите любую папку на своем компьютере, имеющую вложенные директории. Выведите на печать в терминал ее содержимое, как и всех подкаталогов при помощи функции print_docs(directory).
```python
import os

def print_docs(directory):
    all_files = os.walk(directory)
    for catalog in all_files:
        print(f'Папка {catalog[0]} содержит:')
        print(f'Директории: {", ".join(catalog[1])}')
        print(f'Файлы: {", ".join(catalog[2])}')
        print('-' * 40)

print_docs(r' C:\Users\1\PycharmProjects\PythonProject\tema_6')
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/lab_8.png)

## Выводы

Программа создаёт большой список, состоящий из нескольких списков поменьше:

- from random import randint — импортирует генератор случайных целых чисел.
- def list_maker(): — функция создаёт список, где один случайный int повторяется несколько раз.
- a = [randint(1,100)]*randint(3,10) — выбирает число и создаёт список из нескольких копий этого числа.
- return a — возвращает список.
- if __name__ == '__main__': - запускает тестовые вызовы функции.
- result = [] — создаёт пустой список.
- Цикл добавляет несколько списков от list_maker в result.
- print(result) — печатает список списков.


## Лабораторная работа №9
###Требуется реализовать функцию, которая выводит слово, имеющее максимальную длину (или список слов, если таковых несколько). Проверьте работоспособность программы на своем наборе данных.

```python
def longest_word(file):
    with open(file, encoding= 'utf-8') as f:
        word = f.read().split()
        max_length = len(max(word, key=len))
        for w in word:
            if len(w)==max_length:
                sought_word = w
        if len(sought_word)==1:
            return sought_word[0]
        return sought_word
print(longest_word('input.txt'))
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/lab_9.png)

## Выводы

Программа сравнивает множества и определяет зависимость двух множеств друг от друга:

- def superset(...) — функция, сравнивающая множества на супермножества.
- if set_1 > set_2: — проверка, является ли set_1 чистым супермножеством set_2.
- elif set_1 == set_2: — проверяет равенство множеств.
- elif set_1 < set_2: — проверяет, является ли set_1 подмножеством set_2, тогда set_2 — супермножество.
- В строке с print('Объект {set_2} является...') нет f-строки, поэтому не подставится set_2, исправить на f-string.
- if __name__=='__main__': — запускает тестовые вызовы функции.
- Выводит результат сравнения разных пар множеств.

## Лабораторная работа №10
### Требуется создать csv-файл «rows_300.csv» со следующими столбцами:
### № - номер по порядку (от 1 до 300);
### Секунда – текущая секунда на вашем ПК;
### Микросекунда – текущая миллисекунда на часах.
### Для наглядности на каждой итерации цикла искусственно приостанавливайте скрипт на 0,01 секунды.

```python
import csv
import datetime
import time

with open('rows_300.csv', 'w', encoding='utf-8', newline='') as f:
    writer = csv.writer(f)
    writer.writerow(['№','Секунда','Микросекунда'])
    for line in range(1,301):
        writer.writerow([line, datetime.datetime.now().second, datetime.datetime.now().microsecond])
        time.sleep(0.01)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/lab_10.png)

## Выводы

Эта программа "переворачивает" список и выводит элементы в обратном порядке:

- my_list = [...] — создаёт список из четырёх чисел.
- print(my_list[::-1]) — выводит список в обратном порядке срезом.

## Самостоятельная работа №1
### Найдите в интернете любую статью (объем статьи не менее 200 слов), скопируйте ее содержимое в файл и напишите программу, которая считает количество слов в текстовом файле и определит самое часто встречающееся слово. Результатом выполнения задачи будет: скриншот файла со статьей, листинг кода, и вывод в консоль, в котором будет указана вся необходимая информация.

```python
import re
from collections import Counter

f = 'input.txt'
with open(f, 'r', encoding='utf-8') as file:
    text = file.read()

# Очистка текста от пунктуации и разбивка на слова
words = re.findall(r'\b\w+\b', text.lower())
total_words = len(words)
counter = Counter(words)
most_common_word, frequency = counter.most_common(1)[0]

print(f"Общее количество слов: {total_words}")
print(f"Самое часто встречающееся слово: '{most_common_word}' (встречается {frequency} раз)")
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/sam_1.png)

## Выводы

Эта программа вычисляет количество выданных чеков, количество разных посетителей и клиента, посетившего заведение больше всего:

- my_list=[...] - объявление массива с чеками.
- colvo_checks = len(my_list) — подсчёт количества элементов в массиве.
- unique = set(my_list) — создаёт множество из списка, оставляя только уникальные элементы.
- num_unique = len(unique) — считает количество уникальных элементов.
- print(...) — выводит число уникальных посетителей.
- often = Counter(my_list) — создаёт словарь, где ключи — элементы списка, значения — частота их появления.
- most_often = often.most_common(1)[0] — берёт самый частый элемент и его количество (первый из списка самых частых).
- print(...) — выводит элемент, который встречался чаще всех, и количество его появлений.

## Самостоятельная работа №2
### У вас появилась потребность в ведении книги расходов, посмотрев все существующие варианты вы пришли к выводу что вас ничего не устраивает и нужно все делать самому. Напишите программу для учета расходов. Программа должна позволять вводить информацию о расходах, сохранять ее в файл и выводить существующие данные в консоль. Ввод информации происходит через консоль. Результатом выполнения задачи будет: скриншот файла с учетом расходов, листинг кода, и вывод в консоль, с демонстрацией работоспособности программы.

```python
import os

f = 'expen.txt'
def load_expenses():
    """Загрузка списка расходов из файла"""
    expenses = []
    if os.path.exists(f):
        with open(f, 'r', encoding='utf-8') as file:
            for line in file:
                line = line.strip()
                if line:
                    expenses.append(line)
    return expenses


def save_expense(expense_str):
    """Добавление новой записи расхода в файл"""
    with open(f, 'a', encoding='utf-8') as file:
        file.write(expense_str + '\n')


def add_expense():
    """Запрос у пользователя данных для нового расхода и из созранение"""
    date = input("Введите дату (например, 2023-10-20): ")
    amount = input("Введите сумму: ")

    expense_str = f"{date}| {amount}"
    save_expense(expense_str)
    print("Расход добавлен!")


def show_expenses():
    """Вывод всех сохранённых расходов"""
    expenses = load_expenses()
    if not expenses:
        print("Нет записей расходов.")
    else:
        print("\nТекущие расходы:")
        for i, expense in enumerate(expenses, 1):
            print(f"{i}. {expense}")


def main():
    while True:
        print("\nВыберите действие:")
        print("1. Добавить расход")
        print("2. Посмотреть расходы")
        choice = input()

        if choice == '1':
            add_expense()
        elif choice == '2':
            show_expenses()



if __name__ == "__main__":
    main()
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/sam_2.png)

## Выводы

Эта программа помогает вычислить 3 лучших/худших результата и результаты больше 10 из данного массива:

- his_list = [...] — список с результатами.
- besters = sorted(his_list)[:3] — сортирует список по возрастанию и берёт первые три элемента (лучшие результаты — минимальные).
- print(...) — выводит три лучших результата.
- worsters = sorted(his_list)[-3:] — берёт три последних элемента после сортировки (худшие — максимальные).
- print(...) — выводит три худших результата.
- Цикл for проходит по исходному списку и выводит значения ≥ 10 с пробелом.
  
## Самостоятельная работа №3
### Имеется файл input.txt с текстом на латинице. Напишите программу, которая выводит следующую статистику по тексту: количество букв латинского алфавита; число слов; число строк.
### 	Текст в файле: 
### Beautiful is better than ugly. 
### Explicit is better than implicit. 
### Simple is better than complex.
### Complex is better than complicated.
### 	Ожидаемый результат: 
### Input file contains:
### 108 letters
### 20 words
### 4 lines
```python
import re
with open('input.txt', 'r', encoding='utf-8') as f:
    lines = f.readlines()
num_lines = len(lines)
text = ''.join(lines)
words = re.findall(r'\b\w+\b', text)
num_words = len(words)


letters = re.findall(r'[A-Za-z]', text)
num_letters = len(letters)
print(f"Input file contains:\n{num_letters} letters\n{num_words} words\n{num_lines} lines")
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/sam_3.png)

## Выводы

Программа находит площади двух треугольников - с самыми маленькими рёбрами и с самыми большими рёбрами:

- import math — импорт библиотеки для математических функций.
- Переменные one, two, three — списки с числами, представляющими стороны треугольников.
- max_1, max_2, max_3 — наибольшие значения в каждом списке (стороны треугольника с максимальными длинами).
- p — полупериметр треугольника.
- area_1 = math.sqrt(...) — вычисление площади треугольника по формуле Герона.
- print(...) — вывод площади максимального треугольника, округлённой до двух знаков.
- Аналогично для минимальных сторон min_1, min_2, min_3 и площади area_2.
- Вывод площади минимального треугольника.

## Самостоятельная работа №4
### Напишите программу, которая получает на вход предложение, выводит его в терминал, заменяя все запрещенные слова звездочками * (количество звездочек равно количеству букв в слове). Запрещенные слова, разделенные символом пробела, хранятся в текстовом файле input.txt. Все слова в этом файле записаны в нижнем регистре. Программа должна заменить запрещенные слова, где бы они ни встречались, даже в середине другого слова. Замена производится независимо от регистра: если файл input.txt содержит запрещенное слово exam, то слова exam, Exam, ExaM, EXAM и exAm должны быть заменены на ****.
### 	Запрещенные слова:
### hello email python the exam wor is
### 	Предложение для проверки:
### Hello, world! Python IS the programming language of thE future. My EMAIL is....
### PYTHON is awesome!!!!
### 	Ожидаемый результат:
### *****, ***ld! ****** ** *** programming language of *** future. My ***** **....
### ****** ** awesome!!!!

```python
import re

file = 'input.txt'
sentence = "Hello, world! Python IS the programming language of thE future. My EMAIL is....\nPYTHON is awesome!!!!"
with open(file, 'r', encoding='utf-8') as f:
    bad_words = f.read().split()
bad_words = [word.lower() for word in bad_words]

pattern = re.compile('|'.join([re.escape(word) for word in bad_words]), re.IGNORECASE)
def replace_bad_words(text):
    def replacer(match):
        word = match.group()
        if word.lower() in bad_words:
            return '*' * len(word)
        return word
    return pattern.sub(replacer, text)

result = replace_bad_words(sentence)
print(result)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/sam_4.png)

## Выводы

Эта программа позволяет заменить все 2 и 3 на 4:

- Заданы три списка a_list, b_list, c_list.
- Выводится исходный список.
- Цикл проходит по списку, заменяя значения 2 и 3 на 4.
- Печатает элементы с заменами подряд через пробел.
- Аналогично для всех трёх списков.

## Самостоятельная работа №5
### Самостоятельно придумайте и решите задачу, которая будет взаимодействовать с текстовым файлом.
```python
import re
with open('input.txt', 'r', encoding='utf-8') as f:
    content = f.read()
words = re.findall(r'\b\w+\b', content)
print(f"В файле содержится {len(words)} слов.")
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_5/pic/sam_5.png)

## Выводы

Эта программа позволяет преобразовать повторяющееся число в строку, исходя из количества повторений данного элемента:

- from collections import Counter — импорт счётчика количества элементов.
- Определены три списка с повторяющимися числами.
- Функция podschet(posledovatelnost):
- считает частоты элементов с помощью Counter;
- создаёт пустое множество;
- добавляет уникальные числа в множество;
- для чисел с повторениями добавляет в множество строки с количеством повторений (например, "11", "111");
- Каждый список обрабатывается функцией и выводится соответствующее множество.


## Общие выводы по теме

Изучив тему «Работа с файлами в Python», я освоила сохранение данных вне программы для их сохранности и доступности. Научилась открывать файлы в разных режимах (чтение, запись, добавление) и правильно закрывать их или использовать контекстные менеджеры (with). Также я узнала, как читать данные из файлов целиком, построчно или посимвольно и записывать информацию. Эти знания важны для сохранения результатов, обработки внешних данных и ведения журналов. Умение работать с файлами расширяет возможности автоматизации и решения практических задач, развивает навыки работы с внешними данными и повышает уверенность в использовании Python для реальных проектов.
