# Тема 5. Функции и модули
Отчет по Теме #5 выполнила:
- Турылева Диана Игоревна
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |  |
| Задание 7 | + |  |
| Задание 8 | + |  |
| Задание 9 | + |  |
| Задание 10 | + |  |

## Лабораторная работа №1
### Друзья предложили вам поиграть в игру “найди отличия и убери повторения (версия для программистов)”. Суть игры состоит в том, что на вход программы поступает два множества, а ваша задача вывести все элементы первого, которых нет во втором. А вы как раз недавно прошли множества и знаете их возможности, поэтому это не составит для вас труда.
```python
set_1 = {'White', 'Black','Red','Pink'}
set_2 = {'Red','Green','Blue','Red'}
print(set_1 – set_2)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/lab_1.png)

## Выводы

Эта программа выводит произведение чисел 2 и 5:

- def main(): — определяет функцию main, которая при вызове выполняет код внутри.
- print(2*5) — вычисляет 2 * 5 и выводит результат (10) на экран.
- if __name__ == '__main__': — проверяет, выполняется ли скрипт как основная программа .
- main() — вызывается функция main, и в результате на экран выводится число 10.

## Лабораторная работа №2
### Напишите две одинаковые программы, только одна будет использовать set(), а вторая frozenset() и попробуйте к исходному множеству добавить несколько элементов, например, через цикл. 
```python
#вариант 1
a = set('abcdefg')
print(a)
for i in range(1,5):
    a.add(i)
print(a)

#вариант 2
a = frozenset('abcdefg')
print(a)
for i in range(1,5):
    a.add(i)
print(a)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/lab_2.png)

## Выводы

Эта программа возвращает произведение 2 и 5 из функции и выводит его снаружи:

- def main(): — функция main без параметров.
- return 2*5 — вычисляет произведение и возвращает результат (число 10).
- if __name__ == '__main__': — проверка запуска скрипта как основной программы.
- print(main()) — вызывает функцию main, получает результат 10 и выводит его на экран.

## Лабораторная работа №3
### На вход в программу поступает список (минимальной длиной 2 символа). Напишите программу, которая будет менять первый и последний элемент списка
```python
def replace(input_list):
    memory = input_list[0]
    input_list[0] = input_list[-1]

    return input_list

print(replace([1,2,3,4,5]))
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/lab_3.png)

## Выводы

Программа суммирует два числа в функции и выводит результат несколько раз:

- def main(one, two): — функция main с двумя параметрами one и two.
- result = one + two — складывает аргументы и сохраняет сумму в переменной result.
- return result — возвращает сумму.
- Цикл for i in range(5): — повторяет следующие действия 5 раз:
- задаёт переменные x=10, y=5
- вызывает функцию main(x,y), получает сумму (15)
- выводит полученный ответ
- В результате пять раз подряд на экран выводится число 15

## Лабораторная работа №4
### На вход в программу поступает список (минимальной длиной 10 символов). Напишите программу, которая выводит элементы с индексами от 2 до 6. В программе необходимо использовать “срез”.

```python
a = [12,54,32,57,824,13,523,21,451,19]
print(a[2:6])
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/lab_4.png)

## Выводы

Программа принимает обязательный и дополнительные позиционные аргументы, выводит их статистику и возвращает вычисленное значение:

- def main(x, *args): — функция, принимающая один обязательный аргумент x и любое количество дополнительных args.
- one = x — сохраняет первый аргумент.
- two = sum(args) — вычисляет сумму всех дополнительных аргументов.
- three = float(len(args)) — считает число дополнительных аргументов и преобразует в число с плавающей точкой.
- print(f"one={one}\ntwo={two}\nthree={three}") — выводит значения one, two и three.
- return x + sum(args) / float(len(args)) — возвращает результат формулы: первый аргумент плюс среднее арифметическое дополнительных.
- В блоке if __name__ == "__main__": функция вызывается с набором чисел: 5, 5, 7, 0, 1, 1, 2, -4.

## Лабораторная работа №5
### Иван задумался о поиске «бесполезного» числа, полученного из списка. Суть поиска в следующем: он берет произвольный список чисел, находит самое большое из них, а затем делит его на длину списка. Студент пока не придумал, где может пригодиться подобное значение, но ищет у вас помощи в реализации такой функции useless().
```python
def useless(lst):
    return max(lst)/len(lst)

print(useless([3,5,7,3,33]))
print(useless([-12.5,54,77.3,0,-36,98.2,-63,21.7,47,-89.6]))
print(useless([-25.8, 86, 12.5,-56,73.2,0,43,-91.5, 65.9,-7]))
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/lab_5.png)

## Выводы

Программа демонстрирует работу с именованными аргументами **kwargs и их вывод:

- def main(**kwargs): — функция принимает произвольное количество именованных аргументов как словарь kwargs.
- Первый цикл for i in kwargs.items(): перебирает пары (ключ, значение) и выводит их через пробел — например, x [1, 2, 3].
- Затем печатается пустая строка.
- Второй цикл for key in kwargs: выводит пары в формате ключ: значение.
- В блоке проверки if __name__ == "__main__": функция вызывается с пятью именованными аргументами, каждый из которых представляет список чисел, и выводит их двумя способами.
- Затем вызывается снова с двумя аргументами, переданными с помощью распаковки словаря.
  
## Лабораторная работа №6
### Ребята не могут определится каким супергероем они хотят стать. У них есть случайно составленный список супергероев, и вы должны определить кто из ребят будет каким супергероем. Необходимо использовать разделение списков.

```python
uperheroes = ['superman','spiderman','batman']
nikolay, vasiliy,ivan = superheroes
print('Николай - ', nikolay)
print('Василий - ', vasiliy)
print('Иван - ', ivan)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/lab_6.png)

## Выводы

Эта программа вычисляет и выводит среднее значение для каждого списка, переданного через именованные аргументы:

- def main(**kwargs): — функция принимает любое количество именованных аргументов в виде словаря kwargs, где каждое значение — список чисел.
- for i,j in kwargs.items(): — перебирает пары ключ (i) и соответствующий список чисел (j).
- print(f"{i}. Mean ={mean(j)}") — выводит имя аргумента и среднее значение списка, вычисленное функцией mean.
- def mean(data): — функция, которая принимает список data, считает сумму элементов и делит на их количество, возвращая среднее арифметическое.
- В блоке if __name__ == '__main__': вызывается main с двумя списками: x=[1,2,3] и y=[3,6,2].
- В результате выведется:
x. Mean =2.0
y. Mean =3.6666666666666665

## Лабораторная работа №7
### Вовочка, насмотревшись передачи “Слабое звено” решил написать программу, которая также будет находить самое слабое звено (минимальный элемент) и удалять его, только делать он это хочет не с людьми, а со списком. Помогите Вовочке с реализацией программы.

```python
a = [-25.8, 86, 12.5, -56, 73.2, 0, 43, -91.5, 65,9,-7]
a.sort()
print('Отсортированный список:\n', a)
a.pop(0)
print('Отсортированный список без наименьшего элемента:\n', a)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/lab_7.png)

## Выводы

Пример вызова функции из другого файла:

- В первом файле определена функция talk(), которая выводит строку "Let's talk about history!".
- Во втором файле происходит импорт функции talk из первого (from imoirt_file import talk — опечатка, должно быть import_file или корректное имя).
- Проверяется условие запуска основного скрипта if __name__ == "__main__": , если вызвать talk(), функция выведет сообщение.
- Таким образом, показывается механизм импорта функций из других модулей и их вызова.

## Лабораторная работа №8
### Михаил решил создать большой n-мерный список, для этого он случайным образом создал несколько списков, состоящих минимум из 3, а максимум из 10 элементов и поместил их в один большой список. Он также как и Иван не знает зачем ему это сейчас нужно, но надеется на то, что это пригодится ему в будущем.

```python
from random import randint

def list_maker():
    a = [randint(1,100)]*randint(3,10)
    return a

if __name__ == '__main__':
    result = []
    for i in range(randint(1,5)):
        result.append(list_maker())

    print(result)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/lab_8.png)

## Выводы

Программа выполняет вычисления с использованием функций из модуля math на основе пользовательского ввода:

- import math — импорт математического модуля.
- В функции main() происходит запрос числа от пользователя через input и преобразование в целое.
- Выводится квадратный корень числа с помощью math.sqrt(value).
- Затем выводятся синус и косинус числа с помощью math.sin(value) и math.cos(value).
- В блоке if __name__ == '__main__': - вызывается функция main.
- Программа позволяет быстро получить базовые тригонометрические и корневые значения для введённого числа.


## Лабораторная работа №9
### Вы работаете в ресторане и отвечает за статистику покупок, ваша задача сравнить между собой заказы покупателей, которые указаны в разном порядке. Реализуйте функцию superset(), которая принимает 2 множества. Результат работы функции: вывод в консоль одного из сообщений в зависимости от ситуации:
### 1 – «Супермножество не обнаружено»
### 2 – «Объект {X} является чистым супермножеством»
### 3 – «Множества равны»
```python
def superset(set_1,set_2):
    if set_1>set_2:
        print(f'Объект {set_1} является чистым супермножеством')
    elif set_1==set_2:
        print(f'Множества равны')
    elif set_1<set_2:
        print('Объект {set_2} является чистым супермножеством')
    else:	
        print('Супермножество не обнаружено')

if __name__=='__main__':
    superset({1,8,3,5},{3,5})
    superset({1,8,3,5},{5,3,8,1})
    superset({3,5},{5,3,8,1})
    superset({90,100},{3,5})
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/lab_9.png)

## Выводы

Программа работает с датами, вычисляя дату и день недели через n дней от текущей даты:

- Импортируются классы datetime как dt и timedelta как td.
- Функция main() выводит текущую дату (dt.today().date()) и номер дня недели (1-Понедельник, 7-Воскресенье) через isoweekday().
- Запрашивается у пользователя число дней n.
- Вычисляется дата, которая будет через n дней, прибавляя timedelta(days=n) к текущей дате.
- Выводится эта дата и её день недели.
- Проверка if __name__ == "__main__": - запускает main.
- Программа полезна для вычисления и отображения даты и дня недели сдвинутых на заданное количество дней.

## Лабораторная работа №10
### Предположим, что вам нужно разобрать стопку бумаг, но нужно начать работу с нижней, “переверните стопку”. Вам дан произвольный список. Представьте его в обратном порядке. Программа должна занимать не более двух строк в редакторе кода.

```python
my_list = [2,5,8,3]
print(my_list[::-1])
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/lab_10.png)

## Выводы

Эта программа вычисляет площадь прямоугольника или треугольника в зависимости от выбора пользователя:

- Объявляется глобальная переменная result, в которую сохраняется результат.
- Функция rectangle() запрашивает у пользователя ширину и высоту, затем вычисляет площадь как произведение и сохраняет в result.
- Функция triangle() запрашивает основание и высоту, вычисляет площадь треугольника по формуле 0.5 * a * h и сохраняет в result.
- Пользователю предлагается ввести "1" или "2" чтобы выбрать фигуру.
- Если "1", вызывается функция rectangle, если "2" — triangle.
- В конце печатается значение result с подписью "Площадь:".

## Самостоятельная работа работа №1
### Ресторан на предприятии ведет учет посещений за неделю при помощи кода работника. У них есть список со всеми посещениями за неделю. Ваша задача посчитать:
### •	Сколько было выдано чеков
### •	Сколько разных людей посетило ресторан
### •	Какой работник посетил ресторан больше всех раз

```python
from collections import Counter

my_list = [8734, 2345, 8201, 6621, 9999, 1234, 5678, 8201, 8888, 4321, 3365,
1478, 9865, 5555, 7777, 9998, 1111, 2222, 3333, 4444, 5556, 6666,
5410, 7778, 8889, 4445, 1439, 9604, 8201, 3365, 7502, 3016, 4928,
5837, 8201, 2643, 5017, 9682, 8530, 3250, 7193, 9051, 4506, 1987,
3365, 5410, 7168, 7777, 9865, 5678, 8201, 4445, 3016, 4506, 4506]

colvo_checks = len(my_list)
print("Количество чеков: ", colvo_checks)

unique = set(my_list)
num_unique = len(unique)
print("Количество уникальных посетителей: ", num_unique)

often = Counter(my_list) #создаёт объект Counter из списка. Often - словарь, где ключи — коды работников, а значения — сколько раз эти коды встречаются в списке.
most_often = often.most_common(1)[0]    #метод most_common возвращает список, состоящий из кортежей, из самых частых элементов
print("Посетил ресторан больше всех: ", most_often)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/sam_1.png)

## Выводы

Эта программа вычисляет длину гипотенузы для нескольких пар координат и измеряет время выполнения:

- from datetime import datetime — импорт функции для работы с датой и временем.
- from math import sqrt — импорт функции для вычисления квадратного корня.
- def main(**kwargs): — функция принимает любое количество именованных аргументов в виде словаря.
- В цикле for key in kwargs.items(): перебираются пары (имя, список из двух чисел).
- result = sqrt(key[1][0]  2 + key[1][1]  2) — вычисляется длина гипотенузы по теореме Пифагора (корень из суммы квадратов двух координат). Здесь нужно заменить пропущенный оператор возведения в степень на ** вместо просто 2.
- print(result) — выводится длина гипотенузы для каждой пары чисел.
- В блоке if __name__ == '__main__': - фиксируется время начала работы программы.
- Вызывается функция main с несколькими списками координат.
- По окончании работы вычисляется и выводится время исполнения программы (разница между текущим и стартовым временем).

## Самостоятельная работа работа №2
### На физкультуре студенты сдавали бег, у преподавателя физкультуры есть список всех результатов, ему нужно узнать:
### •	Три лучшие результата
### •	Три худшие результата
### •	Все результаты начиная с 10 
### Ваша задача помочь ему в этом.

```python
his_list = [10.2, 14.8, 19.3, 22.7, 12.5, 33.1, 38.9, 21.6, 26.4, 17.1, 30.2, 35.7, 16.9,
27.8, 24.5, 16.3, 18.7, 31.9, 12.9, 37.4]

besters = sorted(his_list)[:3]  #сортировка по возрастанию
print("Три лучших результата: ", besters)

worsters = sorted(his_list)[-3:]    #выбирает 3 последних результата
print("Три худших результата: ", worsters)

for value in his_list:
    if value >= 10:
        print(value, end=' ')
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/sam_2.png)

## Выводы

Эта программа имитирует бросок кубика, с рекурсивным повтором результата при определённых значениях:

- from random import randint — импорт функции для генерации случайных чисел.
- def kubik(): — определение функции броска кубика.
- storona = randint(1,6) — случайным образом выбирается число от 1 до 6 (значение на стороне кубика).
- print(f"Значение на стороне кубика: {storona}") — выводится результат броска.
- Если storona равна 1 или 2, выводится "Вы проиграли".
- Если storona равна 3 или 4, вызывается функция kubik() рекурсивно, то есть бросок повторяется.
- Если значение 5 или 6, выводится "Вы победили".
- В блоке if __name__ == '__main__': - вызывается функция kubik().
  
## Самостоятельная работа работа №3
### Преподаватель по математике придумал странную задачку. У вас есть три списка с элементами, каждый элемент которых – длина стороны треугольника, ваша задача найти площади двух треугольников, составленные из максимальных и минимальных элементов полученных списков. Результатом выполнения задачи будет: листинг кода, и вывод в консоль, в котором будут указаны два этих значения.
```python
import math

one = [12, 25, 3, 48, 71]
two = [5, 18, 40, 62, 98]
three = [4, 21, 37, 56, 84]

max_1 = max(one)
max_2 = max(two)
max_3 = max(three)
p = 0.5*(max_1 + max_2 + max_3)
area_1 = math.sqrt(p*(p-max_1)*(p-max_2)*(p-max_3))
print("Площадь максимального треугольника: ", round(area_1, 2))

min_1 = min(one)
min_2 = min(two)
min_3 = min(three)
polu = 0.5*(min_1 + min_2 + min_3)
area_2 = math.sqrt(polu*(polu-min_1)*(polu-min_2)*(polu-min_3))
print("Площадь минимального треугольника: ",round(area_2, 2))
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/sam_3.png)

## Выводы

Эта программа выводит текущее время с секундной задержкой 5 раз подряд:

- from datetime import datetime — импорт функции для работы с текущей датой и временем.
- import time — импорт модуля для работы со временем и паузами.
- В цикле for i in range(5): выполняется 5 итераций.
- На каждой итерации now = datetime.now() получает текущее время.
- print(now.strftime("%H:%M:%S")) выводит время в формате часы:минуты:секунды.
- time.sleep(1) приостанавливает выполнение программы на 1 секунду, чтобы следующий вывод происходил спустя секунду.

## Самостоятельная работа работа №4
### Никто не любит получать плохие оценки, поэтому Борис решил это исправить. Допустим, что все оценки студента за семестр хранятся в одном списке. Ваша задача удалить из этого списка все двойки, а все тройки заменить на четверки.

```python
a_list = [2, 3, 4, 5, 3, 4, 5, 2, 2, 5, 3, 4, 3, 5, 4]
b_list = [4, 2, 3, 5, 3, 5, 4, 2, 2, 5, 4, 3, 5, 3, 4]
c_list = [5, 4, 3, 3, 4, 3, 3, 5, 5, 3, 3, 3, 3, 4, 4]

print("\n\nСтарый массив\n",a_list)
print("Новый массив: ")
for value in a_list:
    if value == 2 or value == 3:
        value = 4
        new_value = value
        print(new_value, end=' ')
    else:
        new_value = value
        print(new_value, end=' ')

print("\n\nСтарый массив\n",b_list)
print("Новый массив: ")
for valu in b_list:
    if valu == 2 or valu == 3:
        valu = 4
        new_valu = valu
        print(new_valu, end=' ')
    else:
        new_valu = valu
        print(new_valu, end=' ')

print("\n\nСтарый массив\n",c_list)
print("Новый массив: ")
for val in c_list:
    if val == 2 or val == 3:
        val = 4
        new_val = val
        print(new_val, end=' ')
    else:
        new_val = val
        print(new_val, end=' ')
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/sam_4.png)

## Выводы

Эта программа вычисляет среднее арифметическое произвольного количества чисел и выводит результат:

- def srednee(*args): — объявляется функция, которая принимает нефиксированное число аргументов и упаковывает их в кортеж args.
- args = [float(arg) for arg in args] — все переданные аргументы преобразуются в числа с плавающей точкой и превращаются в список.
- col_vo = len(args) — определяется количество переданных аргументов.
- result = sum(args) / col_vo — вычисляется среднее арифметическое: сумма всех чисел делится на их количество.
- print(result) — выводится вычисленное среднее значение.
- В блоке if __name__ == '__main__': - вызывается функция srednee с набором чисел.

## Самостоятельная работа работа №5
### Вам предоставлены списки натуральных чисел, из них необходимо сформировать множества. При этом следует соблюдать это правило: если какое-либо число повторяется, то преобразовать его в строку по следующему образцу: например, если число 4 повторяется 3 раза, то в множестве будет следующая запись: само число 4, строка «44», строка «444».
```python
from collections import Counter

list_1 = [1, 1, 3, 3, 1]
list_2 = [5, 5, 5, 5, 5, 5, 5]
list_3 = [2, 2, 1, 2, 2, 5, 6, 7, 1, 3, 2, 2]

def podschet(posledovatelnost):
    colvo = Counter(posledovatelnost)
    result_set = set()
    for num, count in colvo.items():
        # добавляем сами числа
        result_set.add(num)
        # если число повторяется, добавляем строки длиной от 2 до count
        if count != 1:
            for i in range(2, count + 1):
                result_set.add(str(num) * i)
    return result_set

set_1 = podschet(list_1)
set_2 = podschet(list_2)
set_3 = podschet(list_3)

print(set_1)
print(set_2)
print(set_3)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/Tema_4/pic/sam_5.png)

## Выводы

Этот набор из двух файлов позволяет вычислить площадь треугольника по трём сторонам с помощью формулы Герона:

- import math — импортируется модуль для математических функций.
- def formula(a,b,c): — определяется функция, принимающая длины трёх сторон треугольника.
- p = (a+b+c)/2 — вычисляется полупериметр.
- return math.sqrt(p * (p - a) * (p - b) * (p - c)) — возвращается площадь по формуле Герона: √(p * (p−a) * (p−b) * (p−c)).


## Общие выводы по теме

