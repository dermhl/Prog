# Тема 9. Концепции и принципы ООП
Отчет по Теме #9 выполнила:
- Турылева Диана Игоревна
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |

## Лабораторная работа №1
### Допустим, что вы решили оригинально и немного странно познакомится с человеком. Для этого у вас должен быть написан свой класс на Python, который будет проверять угадал ваше имя человек или нет. Для этого создайте класс, указав в свойствах только имя. Дальше создайте функцию  init (), а в ней сделайте проверку на то угадал человек ваше имя или нет. Также можете проверить что будет, если в этой функции указав атрибут, который не указан в вашем классе, например, попробуйте вызвать фамилию.
```python
class Ivan:
    __slots__ = ['name']
    
    def __init__(self, name):
        if name == 'Иван':
            self.name = f"Да, я {name}"
        else:
            self.name = f"Я не {name}, а Иван"
            
person1 = Ivan('Алексей')
person2 = Ivan('Иван')
print(person1.name)
print(person2.name)

person2.surname = 'Петров'
```

### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_9/pic/lab_1.png)

## Выводы

Эта программа демонстрирует создание объектов с разной инициализацией атрибута name в зависимости от входного значения.:

- __slots__ = ['name'] — ограничивает объекты класса атрибутом name, запрещая добавлять другие атрибуты.
- В конструкторе имя проверяется: если 'Иван', формируется строка "Да, я Иван", иначе — "Я не <имя>, а Иван".
- Создаются два объекта с разными именами, у которых выводится соответствующий атрибут name.
- Добавление surname к person2 приведёт к ошибке, так как surname не разрешён в __slots__.

## Лабораторная работа №2
### Вам дали важное задание, написать продавцу мороженого программу, которая будет писать добавили ли топпинг в мороженое и цену после возможного изменения. Для этого вам нужно написать класс, в котором будет определяться изменили ли состав мороженого или нет. В этом классе  реализуйте  метод,  выводящий  на  печать  «Мороженое  с {ТОППИНГ}» в случае наличия добавки, а иначе отобразится следующая фраза: «Обычное мороженое». При этом программа должна воспринимать как топпинг только атрибуты типа string.

```python
class Icecream:
    def __init__(self, ingridient=None):
        if isinstance(ingridient, str):
            self.ingridient = ingridient
        else:
            self.ingridient = None
    
    def composition(self):
        if self.ingridient:
            print(f"Мороженое с {self.ingridient}")
        else:
            print('Обычное мороженое')
            
icecream = Icecream()
icecream.composition()
icecream = Icecream('шоколадом')
icecream.composition()
icecream = Icecream(5)
icecream.composition()
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_9/pic/lab_2.png)

## Выводы

Этот код реализует класс мороженого с необязательным ингредиентом и выводит состав в зависимости от типа переданного параметра:

- В конструкторе проверяется, является ли параметр строкой, иначе ingridient присваивается None.
- Метод composition выводит разный текст в зависимости от наличия и типа ингредиента.
- Создаются три объекта с разными параметрами: без аргумента, с строкой и с числом.
- Для нестрокового ингредиента выводится "Обычное мороженое".

## Лабораторная работа №3
### Петя – начинающий программист и на занятиях ему сказали реализовать икапсу…что-то. А вы хороший друг Пети и ко всему прочему прекрасно знаете, что икапсу…что-то – это инкапсуляция, поэтому решаете помочь вашему другу с написанием класса с инкапсуляцией. Ваш класс будет не просто инкапсуляцией, а классом с сеттером, геттером и деструктором. После написания класса вам необходимо продемонстрировать что все написанные вами функции работают. Также вас необходимо объяснить Пете почему на скриншоте ниже в консоли выводится ошибка.

```python
class MyClass:
    def __init__(self, value):
        self._value = value
        
    def set_value(self, value): #установка значения атрибута
        self._value = value
        
    def get_value(self):    #получение значения атрибута
        return self._value
        
    def del_value(self):    #удаление атрибута
        del self._value
        
    value = property(get_value,set_value,del_value, 'Свойств del_value')
    
obj = MyClass(42)
print(obj.get_value())
obj.set_value(45)
print(obj.get_value())
obj.set_value(100)
print(obj.get_value())
obj.del_value()
print(obj.get_value())
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_9/pic/lab_3.png)

## Выводы

Программа показывает работу свойства property с методами доступа и удаления атрибута, при этом вызов get после удаления приводит к ошибке AttributeError:

- Создаётся свойство value с методами получения, установки и удаления _value.
- После создания объекта и нескольких изменений значение выводится корректно.
- Метод del_value удаляет атрибут _value.
- При попытке вызвать get_value после удаления _value возникает ошибка AttributeError, так как атрибут уже не существует.

## Лабораторная работа №4
### Вам прекрасно известно, что кошки и собаки являются млекопитающими, но компьютер этого не понимает, поэтому вам нужно написать три класса: Кошки, Собаки, Млекопитающие. И при помощи “наследования” объяснить компьютеру что кошки и собаки – это млекопитающие. Также добавьте какой-нибудь свой атрибут для кошек и собак, чтобы показать, что они чем-то отличаются друг от друга.
```python
class Mammal:
    className = 'Mammal'
    
class Dog(Mammal):
    species = 'canine'
    sounds = 'wow'
    
class Cat(Mammal):
    species = 'feline'
    sounds = 'meow'
    
dog = Dog()
print(f"Dog is {dog.className}, but they say {dog.sounds}")
cat = Cat()
print(f"Cat is {cat.className}, but they say {cat.sounds}")
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_9/pic/lab_4.png)

## Выводы

 Этот пример иллюстрирует наследование классов с общими и уникальными атрибутами, показывая доступ к свойствам родителей и потомков:

- Класс Mammal — родитель со статическим атрибутом className.
- Наследники Dog и Cat имеют уникальные атрибуты species и sounds.
- Создаются объекты dog и cat, у которых выводятся как родительский, так и свои собственные атрибуты.
- Показывается простая иерархия классов и доступ к атрибутам наследников и предка.

## Лабораторная работа №5
### На разных языках здороваются по-разному, но суть остается одинаковой, люди друг с другом здороваются. Давайте вместе с вами реализуем программу с полиморфизмом, которая будет описывать всю суть первого предложения задачи. Для этого мы можем выбрать два языка, например, русский и английский и написать для них отдельные классы, в которых будет в виде атрибута слово, которым здороваются на этих языках. А также напишем функцию, которая будет выводить информацию о том, как на этих языках здороваются. Заметьте, что для решения поставленной задачи мы использовали декоратор @staticmethod, поскольку нам не нужны обязательные параметры-ссылки вроде self.

```python
class Russian:
    @staticmethod
    def greeting():
        print("Привет")
        
class English:
    @staticmethod
    def greeting():
        print("Hello")
        
def greet(language):
    language.greeting()
    
ivan = Russian()
greet(ivan)
john = English()
greet(john)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_9/pic/lab_5.png)

## Выводы

 Код демонстрирует вызов статических методов через объекты разных классов для вывода приветствий на разных языках:

- Классы Russian и English имеют статический метод greeting, выводящий приветствие на соответствующем языке.
- Функция greet вызывает метод greeting у переданного объекта без учёта типа.
- Создаются объекты обоих классов, и для каждого вызывается приветствие с помощью одной функции, демонстрируя полиморфизм.

## Самостоятельная работа №1
### Задание Садовник и помидоры: по описанию создать классы Tomato, TomatoBush, Gardener и провести ряд тестов. В данном задании необходимо вызвать справку по садоводству.
```python
class Tomato:
    states = ["отсутствует", "цветёт", "зелёный", "красный"]  # статическое свойство

    def init(self, index):
        # Динамические свойства, определяются в методе init:
        self._index = index      # индекс текущей стадии (приватное свойство)
        self._state = self.states[self._index]  # текущая стадия (приватное свойство)

    def grow(self):
        if self._index < len(self.states) - 1:
            self._index += 1
            self._state = self.states[self._index]

    def is_ripe(self):
        return self._state == "красный"

class TomatoBush:
    def __init__(self, num):
        self.tomatoes = [Tomato() for _ in range(num)]  # создаём пустые объекты Tomato
        for tomato in self.tomatoes:
            tomato.init(0)  # инициализируем каждый томат в начальном состоянии

    def grow_all(self):
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        self.tomatoes = []

class Gardener:
    def __init__(self, name, plant):
        self.name = name            # публичное свойство
        self._plant = plant         # приватное свойство

    def work(self):
        self._plant.grow_all()

    def harvest(self):
        if self._plant.all_are_ripe():
            print("Все томаты созрели! Урожай собран.")
            self._plant.give_away_all()
        else:
            print("Некоторые томаты еще не созрели. Подождите немного.")

    @staticmethod
    def knowledge_base():
        print("Справка по садоводству:")
        print("1. Следите за стадиями созревания томатов.")
        print("2. Регулярно поливайте растения.")
        print("3. Удаляйте больные и повреждённые плоды.")
        print("4. Собирайте урожай, когда все плоды созреют.")

plant = TomatoBush(2)
gardener1 = Gardener("Иван", plant)
print(gardener1.knowledge_base())
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_9/pic/sam_1.png)

## Выводы

Эта программа моделирует процесс созревания томатов на кусте и выводит справку по садоводству через статический метод:

Класс Tomato
- states = ["отсутствует", "цветёт", "зелёный", "красный"] — статическое свойство, описывающее стадии созревания томата.
- init(self, index) — инициализирует томат с индексом стадии, устанавливая текущий статус _state.
- grow(self) — переводит томат на следующую стадию созревания, если это возможно.
- is_ripe(self) — возвращает True, если томат достиг стадии "красный" (созрел).

Класс TomatoBush
- __init__(self, num) — создаёт список из num объектов Tomato и инициализирует их на стадии 0 ("отсутствует").
- grow_all(self) — переводит все томаты на следующую стадию созревания, вызывая grow() у каждого томата.
- all_are_ripe(self) — проверяет, все ли томаты в кусте достигли стадии "красный" (созрел).
- give_away_all(self) — очищает список томатов (собирает весь урожай).

Класс Gardener
- __init__(self, name, plant) — хранит имя садовника и ссылку на растение (TomatoBush).
- work(self) — садовник ухаживает за растением, вызывая метод grow_all().
- harvest(self) — проверяет созревание всех томатов. Если все созрели, выводит сообщение и собирает урожай (give_away_all()), иначе сообщает, что томаты еще не готовы.
- knowledge_base() — статический метод с основными советами по садоводству (вызывается без создания объекта).

В данном тесте:
- Создаётся куст с 2 томатами и садовник Иван.
- Вызов gardener1.knowledge_base() выводит справку по садоводству.
- Показывает работу статического метода.

## Самостоятельная работа №2
### Задание Садовник и помидоры: по описанию создать классы Tomato, TomatoBush, Gardener и провести ряд тестов. В данном задании необходимо создать объекты классов TomatoBush и Gardener.
```python
class Tomato:
    states = ["отсутствует", "цветёт", "зелёный", "красный"]  # статическое свойство

    def init(self, index):
        # Динамические свойства, определяются в методе init:
        self._index = index      # индекс текущей стадии (приватное свойство)
        self._state = self.states[self._index]  # текущая стадия (приватное свойство)

    def grow(self):
        if self._index < len(self.states) - 1:
            self._index += 1
            self._state = self.states[self._index]

    def is_ripe(self):
        return self._state == "красный"

class TomatoBush:
    def __init__(self, num):
        self.tomatoes = [Tomato() for _ in range(num)]  # создаём пустые объекты Tomato
        for tomato in self.tomatoes:
            tomato.init(0)  # инициализируем каждый томат в начальном состоянии

    def __str__(self):
        return f"Куст с {len(self.tomatoes)} томатами"

    def grow_all(self):
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        self.tomatoes = []

class Gardener:
    def __init__(self, name, plant):
        self.name = name            # публичное свойство
        self._plant = plant         # приватное свойство

    def __str__(self):
        return f"Садовник {self.name} ухаживает за {self._plant}"

    def work(self):
        self._plant.grow_all()

    def harvest(self):
        if self._plant.all_are_ripe():
            print("Все томаты созрели! Урожай собран.")
            self._plant.give_away_all()
        else:
            print("Некоторые томаты еще не созрели. Подождите немного.")

    @staticmethod
    def knowledge_base():
        print("Справка по садоводству:")
        print("1. Следите за стадиями созревания томатов.")
        print("2. Регулярно поливайте растения.")
        print("3. Удаляйте больные и повреждённые плоды.")
        print("4. Собирайте урожай, когда все плоды созреют.")

plant = TomatoBush(2)
gardener1 = Gardener("Иван", plant)
print(plant)
print(gardener1)
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_9/pic/sam_2.png)

## Выводы

Программа создаёт объекты куста и садовника и демонстрирует вывод их представления по умолчанию:

Класс Tomato
- states = ["отсутствует", "цветёт", "зелёный", "красный"] — статическое свойство, описывающее стадии созревания томата.
- init(self, index) — инициализирует томат с индексом стадии, устанавливая текущий статус _state.
- grow(self) — переводит томат на следующую стадию созревания, если это возможно.
- is_ripe(self) — возвращает True, если томат достиг стадии "красный" (созрел).

Класс TomatoBush
- __init__(self, num) — создаёт список из num объектов Tomato и инициализирует их на стадии 0 ("отсутствует").
- grow_all(self) — переводит все томаты на следующую стадию созревания, вызывая grow() у каждого томата.
- all_are_ripe(self) — проверяет, все ли томаты в кусте достигли стадии "красный" (созрел).
- give_away_all(self) — очищает список томатов (собирает весь урожай).

Класс Gardener
- __init__(self, name, plant) — хранит имя садовника и ссылку на растение (TomatoBush).
- work(self) — садовник ухаживает за растением, вызывая метод grow_all().
- harvest(self) — проверяет созревание всех томатов. Если все созрели, выводит сообщение и собирает урожай (give_away_all()), иначе сообщает, что томаты еще не готовы.
- knowledge_base() — статический метод с основными советами по садоводству (вызывается без создания объекта).

В данном тесте:
- Выводит объекты plant и gardener1.
- Без переопределения метода __str__ выводятся системные адреса объектов (что отражает необходимость расширения кода для удобной печати).
  
## Самостоятельная работа №3
### Задание Садовник и помидоры: по описанию создать классы Tomato, TomatoBush, Gardener и провести ряд тестов. В данном задании, используя объект класса Gardener, нужно поухаживать за кустом с помидорами.
```python
class Tomato:
    states = ["отсутствует", "цветёт", "зелёный", "красный"]  # статическое свойство

    def init(self, index):
        # Динамические свойства, определяются в методе init:
        self._index = index      # индекс текущей стадии (приватное свойство)
        self._state = self.states[self._index]  # текущая стадия (приватное свойство)

    def grow(self):
        if self._index < len(self.states) - 1:
            self._index += 1
            self._state = self.states[self._index]

    def is_ripe(self):
        return self._state == "красный"

class TomatoBush:
    def __init__(self, num):
        self.tomatoes = [Tomato() for _ in range(num)]  # создаём пустые объекты Tomato
        for tomato in self.tomatoes:
            tomato.init(0)  # инициализируем каждый томат в начальном состоянии

    def __str__(self):
        return f"Куст с {len(self.tomatoes)} томатами"

    def grow_all(self):
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        self.tomatoes = []

class Gardener:
    def __init__(self, name, plant):
        self.name = name            # публичное свойство
        self._plant = plant         # приватное свойство

    def __str__(self):
        return f"Садовник {self.name} ухаживает за {self._plant}"

    def work(self):
        self._plant.grow_all()

    def harvest(self):
        if self._plant.all_are_ripe():
            print("Все томаты созрели! Урожай собран.")
            self._plant.give_away_all()
        else:
            print("Некоторые томаты еще не созрели. Подождите немного.")

    @staticmethod
    def knowledge_base():
        print("Справка по садоводству:")
        print("1. Следите за стадиями созревания томатов.")
        print("2. Регулярно поливайте растения.")
        print("3. Удаляйте больные и повреждённые плоды.")
        print("4. Собирайте урожай, когда все плоды созреют.")

plant = TomatoBush(2)
gardener1 = Gardener("Иван", plant)
gardener1.work()  # садовник ухаживает за кустом
print("Садовник ухаживает за кустом...")
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_9/pic/sam_3.png)

## Выводы

Эта программа показывает, как садовник ухаживает за кустом, переводя томаты на следующую стадию роста:

Класс Tomato
- states = ["отсутствует", "цветёт", "зелёный", "красный"] — статическое свойство, описывающее стадии созревания томата.
- init(self, index) — инициализирует томат с индексом стадии, устанавливая текущий статус _state.
- grow(self) — переводит томат на следующую стадию созревания, если это возможно.
- is_ripe(self) — возвращает True, если томат достиг стадии "красный" (созрел).

Класс TomatoBush
- __init__(self, num) — создаёт список из num объектов Tomato и инициализирует их на стадии 0 ("отсутствует").
- grow_all(self) — переводит все томаты на следующую стадию созревания, вызывая grow() у каждого томата.
- all_are_ripe(self) — проверяет, все ли томаты в кусте достигли стадии "красный" (созрел).
- give_away_all(self) — очищает список томатов (собирает весь урожай).

Класс Gardener
- __init__(self, name, plant) — хранит имя садовника и ссылку на растение (TomatoBush).
- work(self) — садовник ухаживает за растением, вызывая метод grow_all().
- harvest(self) — проверяет созревание всех томатов. Если все созрели, выводит сообщение и собирает урожай (give_away_all()), иначе сообщает, что томаты еще не готовы.
- knowledge_base() — статический метод с основными советами по садоводству (вызывается без создания объекта).

В данном тесте:
- Садовник вызывает метод work(), который переводит все томаты куста на следующую стадию (рост).
- Показывается процесс "ухода за кустом".

## Самостоятельная работа №4
### Задание Садовник и помидоры: по описанию создать классы Tomato, TomatoBush, Gardener и провести ряд тестов. В данном задании нужно собрать урожай, когда томаты еще не дозрели.

```python
class Tomato:
    states = ["отсутствует", "цветёт", "зелёный", "красный"]  # статическое свойство

    def init(self, index):
        # Динамические свойства, определяются в методе init:
        self._index = index      # индекс текущей стадии (приватное свойство)
        self._state = self.states[self._index]  # текущая стадия (приватное свойство)

    def grow(self):
        if self._index < len(self.states) - 1:
            self._index += 1
            self._state = self.states[self._index]

    def is_ripe(self):
        return self._state == "красный"

class TomatoBush:
    def __init__(self, num):
        self.tomatoes = [Tomato() for _ in range(num)]  # создаём пустые объекты Tomato
        for tomato in self.tomatoes:
            tomato.init(0)  # инициализируем каждый томат в начальном состоянии

    def __str__(self):
        return f"Куст с {len(self.tomatoes)} томатами"

    def grow_all(self):
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        self.tomatoes = []

class Gardener:
    def __init__(self, name, plant):
        self.name = name            # публичное свойство
        self._plant = plant         # приватное свойство

    def __str__(self):
        return f"Садовник {self.name} ухаживает за {self._plant}"

    def work(self):
        self._plant.grow_all()

    def harvest(self):
        if self._plant.all_are_ripe():
            print("Все томаты созрели! Урожай собран.")
            self._plant.give_away_all()
        else:
            print("Некоторые томаты еще не созрели. Подождите немного.")

    @staticmethod
    def knowledge_base():
        print("Справка по садоводству:")
        print("1. Следите за стадиями созревания томатов.")
        print("2. Регулярно поливайте растения.")
        print("3. Удаляйте больные и повреждённые плоды.")
        print("4. Собирайте урожай, когда все плоды созреют.")

plant = TomatoBush(2)
gardener1 = Gardener("Иван", plant)
result = gardener1.harvest()  # попытка сборки урожая
if not result:
    print("Томаты еще не дозрели! Продолжаем ухаживать...")
    gardener1.work()
    print("Садовник продолжает уход за кустом.")
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_9/pic/sam_4.png)

## Выводы

Программа пытается собрать урожай и при незрелости томатов инициирует дополнительный уход:

Класс Tomato
- states = ["отсутствует", "цветёт", "зелёный", "красный"] — статическое свойство, описывающее стадии созревания томата.
- init(self, index) — инициализирует томат с индексом стадии, устанавливая текущий статус _state.
- grow(self) — переводит томат на следующую стадию созревания, если это возможно.
- is_ripe(self) — возвращает True, если томат достиг стадии "красный" (созрел).

Класс TomatoBush
- __init__(self, num) — создаёт список из num объектов Tomato и инициализирует их на стадии 0 ("отсутствует").
- grow_all(self) — переводит все томаты на следующую стадию созревания, вызывая grow() у каждого томата.
- all_are_ripe(self) — проверяет, все ли томаты в кусте достигли стадии "красный" (созрел).
- give_away_all(self) — очищает список томатов (собирает весь урожай).

Класс Gardener
- __init__(self, name, plant) — хранит имя садовника и ссылку на растение (TomatoBush).
- work(self) — садовник ухаживает за растением, вызывая метод grow_all().
- harvest(self) — проверяет созревание всех томатов. Если все созрели, выводит сообщение и собирает урожай (give_away_all()), иначе сообщает, что томаты еще не готовы.
- knowledge_base() — статический метод с основными советами по садоводству (вызывается без создания объекта).

В данном тесте:
- Попытка собрать урожай через harvest().
- Если томаты ещё не созрели (all_are_ripe() вернул False), выводится подсказка продолжать ухаживать, и садовник снова заботится о кусте (work()).
- Демонстрирует логику повторного ухода и проверки созревания.

## Самостоятельная работа №5
###  Задание Садовник и помидоры: по описанию создать классы Tomato, TomatoBush, Gardener и провести ряд тестов. В данном задании нужно собрать урожай.
```python
class Tomato:
    states = ["отсутствует", "цветёт", "зелёный", "красный"]  # статическое свойство

    def init(self, index):
        # Динамические свойства, определяются в методе init:
        self._index = index      # индекс текущей стадии (приватное свойство)
        self._state = self.states[self._index]  # текущая стадия (приватное свойство)

    def grow(self):
        if self._index < len(self.states) - 1:
            self._index += 1
            self._state = self.states[self._index]

    def is_ripe(self):
        return self._state == "красный"

class TomatoBush:
    def __init__(self, num):
        self.tomatoes = [Tomato() for _ in range(num)]  # создаём пустые объекты Tomato
        for tomato in self.tomatoes:
            tomato.init(0)  # инициализируем каждый томат в начальном состоянии

    def __str__(self):
        return f"Куст с {len(self.tomatoes)} томатами"

    def grow_all(self):
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        self.tomatoes = []

class Gardener:
    def __init__(self, name, plant):
        self.name = name            # публичное свойство
        self._plant = plant         # приватное свойство

    def __str__(self):
        return f"Садовник {self.name} ухаживает за {self._plant}"

    def work(self):
        self._plant.grow_all()

    def harvest(self):
        if self._plant.all_are_ripe():
            print("Все томаты созрели! Урожай собран.")
            self._plant.give_away_all()
        else:
            print("Некоторые томаты еще не созрели. Подождите немного.")

    @staticmethod
    def knowledge_base():
        print("Справка по садоводству:")
        print("1. Следите за стадиями созревания томатов.")
        print("2. Регулярно поливайте растения.")
        print("3. Удаляйте больные и повреждённые плоды.")
        print("4. Собирайте урожай, когда все плоды созреют.")

plant = TomatoBush(4)
gardener1 = Gardener("Иван", plant)
for _ in range(3):
    gardener1.work()
result = gardener1.harvest()
if result:
    print("Урожай собран:", result)
else:
    print("Томаты еще не готовы к сбору.")
```
### Результат.
![Меню](https://github.com/dermhl/Prog/blob/tema_9/pic/sam_5.png)

## Выводы

Эта программа имитирует несколько циклов ухода за кустом и успешный сбор урожая после созревания всех томатов:

Класс Tomato
- states = ["отсутствует", "цветёт", "зелёный", "красный"] — статическое свойство, описывающее стадии созревания томата.
- init(self, index) — инициализирует томат с индексом стадии, устанавливая текущий статус _state.
- grow(self) — переводит томат на следующую стадию созревания, если это возможно.
- is_ripe(self) — возвращает True, если томат достиг стадии "красный" (созрел).

Класс TomatoBush
- __init__(self, num) — создаёт список из num объектов Tomato и инициализирует их на стадии 0 ("отсутствует").
- grow_all(self) — переводит все томаты на следующую стадию созревания, вызывая grow() у каждого томата.
- all_are_ripe(self) — проверяет, все ли томаты в кусте достигли стадии "красный" (созрел).
- give_away_all(self) — очищает список томатов (собирает весь урожай).

Класс Gardener
- __init__(self, name, plant) — хранит имя садовника и ссылку на растение (TomatoBush).
- work(self) — садовник ухаживает за растением, вызывая метод grow_all().
- harvest(self) — проверяет созревание всех томатов. Если все созрели, выводит сообщение и собирает урожай (give_away_all()), иначе сообщает, что томаты еще не готовы.
- knowledge_base() — статический метод с основными советами по садоводству (вызывается без создания объекта).

В данном тесте:
- Куст с 4 томатами. Садовник последовательно ухаживает 3 раза (повышая стадии созревания).
- После этого вызывается сбор урожая.
- Если все томаты созрели, выводится сообщение об успешном сборе, иначе — что томаты не готовы.
- Демонстрирует полный цикл роста и сбора плодов.


## Общие выводы по теме

Вспомнив основы ООП, я углубила свои знания в принципах объектно-ориентированного программирования на Python. На практике освоила создание объектов с помощью классов, что структурирует код и делает его понятнее. Особое внимание уделила наследованию, позволяющему создавать новые классы на базе существующих. Это упрощает повторное использование кода и снижает дублирование. Изучила инкапсуляцию, защищающую внутренние данные объектов от внешних изменений и повышающую безопасность и надежность программы. Полиморфизм дал возможность реализовывать одни и те же методы по-разному для разных классов, делая программы гибкими и легко расширяемыми. Все эти принципы помогают создавать понятные, структурированные и устойчивые к изменениям программы, облегчая моделирование реальных процессов и решение сложных задач.
